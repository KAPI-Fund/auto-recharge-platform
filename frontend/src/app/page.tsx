import type { Metadata } from "next";
import { HomePage } from "@/components/home-page";

export const metadata: Metadata = {
  title: "ChatGPT 订阅自动开通 | KC ChatGPT PLUS",
};

export default HomePage;
